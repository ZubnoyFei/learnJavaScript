let name = "Ilya";

//alert(`hello ${1}`);// Выводит единицу

//alert(`hello ${"name"}`)// Выводит hello name, т.к. name в данном случае это строка,а не переменная

// alert(`hello ${name}`); Вытакскивает значение переменной name, в данном случае Ilya